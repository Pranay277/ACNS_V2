# Backend dependencies pinned to EXACT versions (P2-05).
#
# Exact pins make production installs reproducible and auditable. To upgrade a
# package deliberately, bump the version here, re-run the test suite and run
# `pip-audit -r requirements.txt` in CI. A full transitive lockfile can be
# generated with pip-tools (`pip-compile`) if a stricter supply-chain posture
# is required.
fastapi==0.141.1
uvicorn==0.34.0
pydantic==2.13.4
firebase-admin==7.2.0
requests==2.33.0
python-dotenv==1.2.2
